services:
  vdoninja:
    container_name: vdoninja
    build: ./vdoninja
    image: vdoninja
    ports:
    - 80:80

  node_mongoose:
    container_name: node_mongoose
    build: ./node_mongoose
    image: node_mongoose
    tty: true
    network_mode: "host"
    restart: always